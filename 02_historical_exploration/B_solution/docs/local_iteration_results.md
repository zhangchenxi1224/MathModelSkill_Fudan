# 本轮本地单因素迭代结果

最终候选为两题局部检测上限L=1并保留joint联合调度；Q3使用原Solver测点，Q4使用冻结NoSignalSolver。清除认证、覆盖、有限光学兜底和原核心文件未修改。新官方演练验证由主执行器执行，本文件只报告本地结果。

|阶段|预先冻结的单一变化|development结果|冻结后的确认与决定|
|---|---|---|---|
|1|L5改为L1/L2/L3，其余完全相同|两题L1按最差池/组成均值优先，L2次选；所有开发设计组L1均值与CI上界为负|冻结L1后只审L1/L5，200世界400运行，8设计组CI上界均负；保留L1|
|2|固定L1，normal比NoSignal|Q3耗时逐例相同，保留normal；Q4四设计组均值负，有2组CI跨0|冻结normal/nosignal后，P3一臂40运行、P4两臂80运行；P4三组CI跨0，未触发预定拒绝门槛，带不确定性保留NoSignal|
|3|固定前两步，只比joint/immediate/scan_then_clear|16个设计对比均值均慢于joint，15个95%区间下界为正，两个替代方案都未通过开发门槛|冻结joint/joint后80新世界各执行保留方案，全部成功；不存在调度改进对比|

L1仍有单例退步。Q4 development最坏退步1279.60秒；独立诊断表明该例移动和光学兜底增加，不能把平均节约解释为逐例改善。L1确认中Q4 broad池仍有单例退步1739.41秒。NoSignal的Q4确认三个区间跨零，当前证据不支持“稳定显著更优”的表述。每池/题号/组成/机制完整数据见各阶段development与confirmation的paired_summary和review_design_groups。

|本地批次|物理运行数|8进程批次墙钟秒|
|---|---:|---:|
|mechanism_primary|800|309.6305|
|mechanism_broad_reference|160|64.0062|
|stage1_all_arms|4000|996.5376|
|stage2_development|640|129.3171|
|stage2_confirmation|120|23.3127|
|stage3_development|960|182.1251|
|stage3_confirmation|80|18.2354|

策略迭代共5800次物理运行、1800个不同世界；机制检查另960次。本轮主要本地合计6760次，全部报告完成且事后外包核验零违规，各批次源和输入散列无漂移。批次墙钟累计1723.1647秒（28.72分钟），不含官方采集、人工分析、快照及独立审计耗时。

Stage1的800次确认分区物理运行已包含在4000中，其中只在L1冻结后读取L1/L5的400次；L2/L3确认性能没有用于后续选择。重新生成确认摘要不会新增物理运行。Stage2确认因Q3保留基线而只有120次；Stage3保留joint只运行80次，不能把一臂结果伪造为配对收益。早期fixture、单测、敏感性和诊断重放不计入上述6760。

三个阶段使用不同世界，不能把各阶段差值相加为端到端提升率。calibrated池只使用冻结的smoothed/broad组成与两个unresolved机制，broad/stress保留更广合法假设；其混合均值不是官方总体期望。最终效果等待新官方独立基线/候选案例检验，正式测试仍未授权。

可复核摘要为`results/round1/local_iteration_summary.json`，含精确策略配置、源散列、选择链散列、各批次元数据与审计路径。
