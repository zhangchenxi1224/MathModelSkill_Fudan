# 验证记录

2026-09-11，Windows 本地，Python `D:\st_python\python.exe`。

`python -m pytest -q tests`：96 passed in 16.48 s。

- 25 项连续光学覆盖构造检查：包围框、整格排除、退化几何、点数与原方案成本比较。
- 15 项路线检查：未访问后缀点集/索引保持、服务目标估计、禁止共享观测的隐含额外预算。
- 13 项首命中排序检查：完整多重集合保持、重复点与零样本点保留、确定性排序与边界输入。
- 27 项实验框架审计：原冠军完整动作/反馈/虚拟成本一致，固定误差场精确重放，失败罚时，源代码快照隔离，分区与冻结选择摘要，双题强冠军控制与单题获选范围。
- 16 项单场景 CLI 检查：真实子进程全清、错误输入与已有目录拒绝、模拟器和源码篡改拒绝，运行中源码漂移使结果失效。

测试是实现契约的证据，不代替完整世界回放效果。大批量回放另逐次要求环境全清、退出证据、全轨迹凸域包含真实源以及虚拟计时复算一致。输入数据集原校验器另验证了 382 项文件摘要、360 个世界摘要、360 组接口重放；结果保存在 `dataset_verification.json`。

每轮实际执行 `runs/<stage>/source` 的独立源码快照和 `runtime` 的冻结模拟器。根目录后续修改不能改变正在执行的候选。结果保存完整规格、世界摘要、源码摘要、计划摘要以及压缩请求/决策日志。确认前冻结每题一个规格，并禁止截取确认集或在冻结后改源代码/登记世界。

## 交付工具的独立 fixture 测试

`tests/test_audit_final.py`：18 passed in 36.68 s。自造请求/决策轨迹检验独立重算、世界误差重放、来源与完整性拒绝、失败罚时、冻结选优和配对统计；准备期间未读取真实确认结果。

`tests/test_package_release.py`：28 passed、1 skipped。检验交付前置门禁、路径范围、精确文件集合、逐文件 SHA256 回读和已有目标拒绝。跳过项是本机无权限创建符号链接的 fixture；其他路径逃逸检查已通过。要求独立审计 `audit_passed=true`、整数 `issue_count=0`，且最终验证 `passed=true`，才允许创建正式包。

## 真实冻结版本的交付检查

两题各取一个原开发场景，使用最终 `solve_local.py` 分别运行获选策略和强冠军，4/4全清；4次虚拟总耗时与full阶段同世界、同策略的结果逐数完全一致。见 `reports/delivery_smoke_verified.json`。这4次交付检查另计，不混入6576次主实验。

最终独立日志复核完成：2556/2556全清，662782条唯一接受请求、333683次真实源外包检查均通过；逐请求微秒计时最大残差为0。确认区间另以独立种子的10000次配对bootstrap复算，结果见 `reports/independent_audit.json`。
