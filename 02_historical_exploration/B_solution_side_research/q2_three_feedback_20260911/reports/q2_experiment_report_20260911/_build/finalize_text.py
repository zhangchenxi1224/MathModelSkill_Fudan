from pathlib import Path
import re

report = Path(__file__).resolve().parent.parent
text = (report / '第二问实验报告.md').read_text(encoding='utf-8')
text = text.replace('# 第二问两次测向定位策略实验报告', '# 第二问三反馈测点选择与定位实验报告')
start = text.index('本报告整理')
end = text.index('## 1 原理分析')
text = text[:start] + '''本研究针对已获得一次方向报告的全向干扰源，建立第二测点选择与两测后定位模型。以两次反馈后相容源位置集合的最小包围圆半径衡量定位不确定性，在全部合法第二反馈中控制最坏值。模型同时利用方向、近距和无信号信息，第二测点只依赖首次公开位置及报告，不依赖真实源位置或未来反馈。

实验采用圆弧几何评价、自适应合法报告分区及多方法测点搜索。在 8 个首次上下文中，新搜索测点的最坏半径数值上界均小于对照精度测点的下界；标准上下文上界由 55.73 米降至 50.29 米。480 个合成物理与误差组合、1920 条配对记录均通过真源覆盖检验，但平均半径由 27.71 米升至 28.30 米。结果支持当前模型下指定测点的最坏精度改善，未证明连续全域最优，也不代表新测点的官方实测成绩。[3–5]

''' + text[end:]
start = text.index('### 2.3 保证接收主线的候选区域')
end = text.index('## 3 模型求解')
text = text[:start] + r'''### 2.3 优化目标和约束

对给定第二测点 q，令 Y(q) 为后验非空的合法反馈集合。先观察反馈、再选择估计圆心，因此完整的决策顺序是

$$
F(q)=\sup_{o\in\mathcal Y(q)}\min_{c\in\mathbb R^2}\sup_{g\in P_2(q,o)}\|g-c\|_2 .
$$

第二测点的优化问题为

$$
F^*=\inf_{q\in\mathbb R^2}F(q).
$$

外层在测量前选择 q，中间层考虑其可能得到的反馈，内层在反馈后选择包围圆心。真实源只要满足设备规则，就属于对应后验，从而到所输出圆心的距离不超过报告的半径上界。

| 模型要素 | 明确规定 |
|---|---|
| 可用信息 | 首次检测坐标和方向报告 |
| 源位置约束 | 全局半径 1800 米的目标圆内 |
| 接收半径约束 | 同一个未知 R 位于 1000 至 1500 米 两次共享 |
| 测向约束 | 返回角误差不超过 1° 方向报告落在 0.01° 格点 |
| 第二测点约束 | 平面位置可选 允许无信号 必须评价全部合法反馈 |
| 定位目标 | 最小化第二反馈后的最坏包围圆半径 |
| 圆心约束 | 收到实际反馈后确定 不要求圆心属于相容源位置集 |
| 其他量 | 路程 时间 样本均值和 20 米达标率均未加入本轮选点目标 |

题面没有给出使期望误差可计算的联合概率分布，因此本模型不将样本均值代入目标函数，也不假设同点重复会降噪。允许无信号不等于忽略它：其后验半径必须同方向、近距分支一起参与最大值。只有显式保留全部合法分支，最坏评价才完整。

### 2.4 第二测点的候选区域

在任一首次相容源位置 g 下，都可以取合法 R＝1500 米。故至少存在一个相容状态能在 q 接收到信号，当且仅当 q 属于以下潜在接收候选域：

$$
Q_{\mathrm{cand}}=\bigcup_{g\in P_1}B(g,1500).
$$

域外任一点对所有相容状态都返回无信号，后验仍是 P₁，不会改善定位。首次点 s₁ 已在该候选域内，重复首次报告提供相同的无信息基准。因此在没有路程目标的本模型中，将搜索限制到该域不会损失更优解。

由 g 距 s₁ 不超过 1500 米且距全局原点不超过 1800 米，三角不等式给出

$$
Q_{\mathrm{cand}}\subseteq B(s_1,3000)\cap B(0,3300).
$$

程序使用包住这一交集的矩形作为有限搜索范围。局部坐标中设全局目标圆心变换为 t，则矩形的横向范围为 max(−3000,tₓ−3300) 至 min(3000,tₓ+3300)，纵向范围同理。该矩形包含额外测点，但不会排除潜在接收候选域；实际评价器会对额外测点完整计算反馈。Qcand 表示至少一种状态可接收，并不要求所有状态都能接收。

''' + text[end:]
start = text.index('### 3.4 如何用于保证接收主线')
end = text.index('## 4 结果检验与输出')
text = text[:start] + text[end:]
text = text.replace('冻结的保证接收精度策略', '冻结的精度对照策略')
text = text.replace('近距属于已接收到信号，但不保证得到第二个方向读数；本报告的“保证接收”包含这一情况。', '近距提供直接的距离上界，即使没有方向读数也可以定位。')
text = text.replace('第二报告“不知道”指尚未前往 q 测量，所以当前无法预知返回方向，或者是否出现近距、无信号。', '选择 q 时尚未执行第二次测量，无法预知返回方向，也无法预知是否出现近距或无信号。')
text = text.replace('原 fixed、原 precision', '对照 fixed、对照 precision')
text = text.replace('旧点也经同一三反馈评价器计算；其安全接收性质使无信号分支为空。新点不能因为半径更小就被认作保证接收模型的合法改进。', '')
start = text.index('这种比较表明模型 B')
end = text.index('### 4.3 合成配对检验')
text = text[:start] + '''对照测点和搜索测点均使用同一 1° 误差、合法报告格点和圆弧后验评价器。最终新点上界小于对照点下界，支持本表中固定测点之间的最坏半径改善；并不证明搜索已经遍历全部连续位置。

''' + text[end:]
text = text.replace('这一“新评价器加保证接收约束”的重搜索尚未产生本报告中的新结果。', '')
start = text.index('这里存在三个不同层次的结论。')
end = text.index('### 4.5 可交付输出及下一轮实验主线')
text = text[:start] + '''物理状态投影和候选域推导回答模型为何成立；固定候选的数值区间回答该点评价是否足够精细；连续所有测点上的全局最优性则需要额外的全域下界。三者不能互相替代。解析几何采用带外扩余量的双精度计算，独立覆盖提供数值复核，未进行形式化证明助手认证。

以 center 为例，新测点约 50.12 至 50.29 米的区间限制的是该点的 F(q)，不是连续最优值 F*。仍可能存在具有更小最坏半径的其他测点。当前搜索记录的全域下界仅为非负半径给出的 0；尚无空间分支定界结果，不能宣称达到全局最优。

''' + text[end:]
start = text.index('### 4.5 可交付输出及下一轮实验主线')
end = text.index('## 附录 复现与证据文件')
text = text[:start] + '''### 4.5 定位输出和实验结论

实际执行时，算法分两阶段输出。选择阶段返回第二测点 q，以及该点全部合法反馈的最坏半径评价区间。接收实际反馈后，重建对应 P₂，返回估计圆心、保证半径和反馈类型。若保证半径不超过 20 米，可由设备规则判定移动到圆心后能进入光学定位范围；本报告没有把尚未发生的最坏报告当作实测反馈。

本轮实验支持以下结论：三反馈最坏半径模型能够仅用首次信息生成第二测点；在所测 8 个上下文中，最终搜索点均严格优于精度对照点的数值界；480 个配对组合没有发现漏包真源；最坏精度收益伴随本批样本均值小幅上升。合成样本是人工敏感性设计，特别是没有系统覆盖 5 至 100 米近端源距，也不能推断官方源与误差分布。归档上下文的结果属于离线模型评价，本轮新增官方调用为 0。

这些结果构成当前策略的实验依据，结论限于已定义模型与已记录数据，尚未建立连续全域最优性或新测点的官方实测性能。

''' + text[end:]
text = text.replace('以上命令不会恢复保证接收硬约束，不代表模型 A 已重跑。', '')
text = re.sub(r'\[3\] 保证接收主线理论：.*?\n\n', '', text)
text = text.replace('[4–6]', '[3–5]').replace('[4,5]', '[3,4]').replace('[6,7]', '[5,6]')
text = text.replace('[4]', '[3]').replace('[5]', '[4]').replace('[6]', '[5]').replace('[7]', '[6]')
# Replace only the exact old single references above; fix new abstract and combined references explicitly.
(report / '第二问实验报告_最终稿.md').write_text(text, encoding='utf-8')
print('Final manuscript prepared')
