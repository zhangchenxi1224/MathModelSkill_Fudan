P0: 模拟器通信接口说明及编程指南
P1: 1. 基本设定
P2: 1.1. 目标区域与坐标系
P3: 测试在一个二维虚拟目标区域中进行。
P4: 目标区域是半径 1800 米的圆形区域。
P5: 所有干扰源位于目标区域内。
P6: 目标区域圆心是坐标原点 (0,0)。
P7: x 轴正方向为东。
P8: y 轴正方向为北。
P9: 坐标单位为米。
P10: 机器狗可以向模拟器提交目标区域外的位置；模拟器仍会按提交的位置计算移动耗时和检测、清除结果。
P11: 机器狗位置不构成正常策略约束，但每个坐标分量必须是有限数值，且绝对值不得超过2000000米。NaN、正负无穷大和超出范围的坐标返回HTTP 400，请求不执行，也不推进位置或虚拟时间。
P12: 1.2. 角度约定
P13: 所有方位角都采用以下约定：
P14: 正东方向为 0∘。
P15: 逆时针方向为正。
P16: 正北方向为 90∘。
P17: 正西方向为 180∘。
P18: 正南方向为 270∘。
P19: 角度范围为 [0,360)。
P20: /measure 返回的 svd_deg 表示干扰源的示向度，单位为度。
P21: 1.3. 频道
P22: 频道用整数表示。
P23: 有效频道范围是 {1,2,⋯,20}。
P24: 每个频道最多对应一个干扰源。
P25: 一次测试中干扰源总数量在 10 到 16 个之间。
P26: 具体有多少个干扰源不会通过接口直接返回，机器狗程序需要根据检测和清除结果自行判断。
P27: /measure 的 channel 表示测向机要检测的频道。
P28: /clear 的 channel 表示要尝试清除的目标干扰源频道。
P29: 注意：/clear 的 channel 不会改变测向机当前频道，也不会产生切换频道耗时。
P30: 1.4. 初始状态
P31: 机器狗成功调用 /enter 后，模拟器初始状态如下，并开始计时：
P32: 初始位置：(0,0)。
P33: 测向机初始频道：1。
P34: /enter 本身不推进虚拟时钟。
P35: 每次测试只允许一个与当前登录参赛队号一致的机器狗进入。机器狗必须逐次发送动作，并在收到本次完整HTTP响应后再发送下一个新动作。正常串行合法请求没有请求速率上限；每次检测的5秒只增加虚拟时间，不要求在现实中等待5秒。
P36: 1.5. 其他信息
P37: 模拟器默认服务地址：http://127.0.0.1:2026。
P38: 选手可以在模拟器空闲时修改端口。
P39: 模拟器只监听本机回环地址，其他电脑不能直接访问该接口。
P40: 非测试期间、5秒倒计时期间以及测试结束后，机器狗接口未开放，连接可能直接失败。
P42: 2. 关键物理规则
P43: 2.1. 最大可被有效接收距离
P44: 每个干扰源都有一个最大可被有效接收距离，简称有效接收半径。
P45: 该距离在 1000 米到 1500 米之间。
P46: 距离超过该干扰源的有效接收半径时，机器狗在该位置接收不到该频道信号。
P47: 该距离不是固定常数，不同干扰源可能不同。
P48: 模拟器接口不会返回某个干扰源的有效接收半径。
P49: 2.2. 全向与定向干扰源
P50: 干扰源分为全向和定向两类。
P51: 全向干扰源：检测点到干扰源的距离不超过有效接收半径时，可以检测到信号。
P52: 定向干扰源：检测点必须同时满足距离条件，并且位于该干扰源的有效覆盖角度范围内，才能检测到信号。有效覆盖角度范围为 180∘（包含边界），定向方向未知。
P53: 因此，/measure 返回无信号时，只能说明当前位置接收不到该频道信号。可能原因包括：
P54: 该频道没有未清除干扰源。
P55: 距离超过有效接收半径。
P56: 该频道干扰源是定向干扰源，但当前位置不在它的覆盖范围内。
P57: 2.3. 示向度误差
P58: /measure 返回的 svd_deg 是示向度，不是干扰源的精确真实方位。
P59: svd_deg 表示在检测点处测得的干扰源的示向度，单位为度。
P60: 模拟器返回的 svd_deg 与干扰源的最大场强方向的真实方位角之间存在一定的误差，参见题目正文。
P61: 误差范围为 [-1∘, 1∘]。
P62: 返回值保留两位小数。
P63: 返回值仍按 [0,360) 归一化。
P64: 机器狗程序不应把单次 svd_deg 当作完全精确的真实方位。
P65: 2.4. 近距离阈值与清除半径
P66: 近距离阈值为 5 米。检测距离不超过 5 米并且位于干扰源信号覆盖角度范围内时，由于信号太强，将无法获得示向度信息，/measure 返回 measure_result="near"，不返回 svd_deg。
P67: 清除半径为 20 米。清除位置与目标干扰源距离小于等于 20 米时，/clear 可以清除成功。注：光学精确定位及清除操作是否成功仅与清除点与干扰源之间的距离有关，与定向干扰源的朝向无关。
P69: 3. 指令
P70: 机器狗程序只需要调用 4 条 HTTP 接口：
T71R0: 指令 | 含义 | 是否推进虚拟时钟
T71R1: POST /enter | 进入目标区域，开始本次运行。 | 否
T71R2: POST /measure | 到指定位置，对指定频道检测。 | 是
T71R3: POST /clear | 到指定位置，尝试清除指定频道的干扰源。 | 是
T71R4: POST /exit | 退出目标区域，结束本次运行。 | 否
P72: 移动和切换频道没有单独的接口：
P73: 移动由 /measure 或 /clear 请求中的 position 体现。
P74: 切换测向机频道只由 /measure 请求中的 channel 体现。
P77: 4. 虚拟时间计算方法
P78: 4.1.虚拟时钟推进规则
P79: 只有请求被模拟器接受，即响应中 accepted=true 时，才可能推进虚拟时钟。
P80: /enter：accepted=true 时也不推进虚拟时钟。
P81: /exit：accepted=true 时也不推进虚拟时钟。
P82: /measure：accepted=true 时推进虚拟时钟。
P83: /clear：accepted=true 时推进虚拟时钟。
P84: accepted=false：本次请求不生效，虚拟时钟不推进。
P85: accepted=false 的响应里 virtual_time_s 为 0。不要把这个 0 当作当前虚拟时刻。需要记录虚拟时刻时，应使用最近一次 accepted=true 的响应。
P86: virtual_time_s 是JSON number，可能带小数，不应按整数解析。模拟器内部按微秒累计，响应最多保留6位小数，并删除无意义的末尾零。
P87: 4.2. 移动耗时
P88: 模拟器会记录机器狗上一次合法动作的位置。
P89: 一次合法 /measure 或 /clear 的移动耗时为
P90: 移动耗时=两次位置之间的直线距离移动速度
P91: 默认移动速度：5 m/s
P92: 例如，从 (0,0) 移动到 (300,400)，直线距离为 500 米，移动耗时为
P93: 5005=100 (秒)
P94: 4.3. 检测耗时
P95: 一次合法 /measure 的总耗时为
P96: 检测总耗时=移动耗时+切换频道耗时+检测动作耗时
P97: 默认值：
P98: 检测动作耗时：5 秒。
P99: 切换频道耗时：1 秒。
P100: 切换频道耗时只看合法 /measure：
P101: 如果本次合法 /measure 的 channel 与测向机当前频道不同，切换频道耗时为 1 秒。
P102: 如果频道相同，切换频道耗时为 0 秒。
P103: 合法 /measure 完成后，测向机当前频道会更新为本次检测频道。
P104: 4.4. 清除耗时
P105: 一次合法 /clear 的总耗时为
P106: 清除总耗时=移动耗时+清除动作耗时
P107: 默认值：
P108: 未发现可清除目标时，只有精确定位动作，没有清除动作，耗时为 3 秒。
P109: 成功清除时，精确定位与清除两个动作总耗时为 5 秒。
P110: /clear 不产生切换频道耗时，也不会改变测向机当前频道。
P111: 4.5. 现实时间和虚拟时间
P112: 用户在模拟器中确认开始后，模拟器先准备数据。数据准备完成后显示5秒倒计时。倒计时结束时开启25分钟测试窗口并开放机器狗接口。
P113: 机器狗成功调用 /enter 后开始计算最长20分钟的程序运行时间。实际现实截止时刻是“25分钟窗口截止”和“/enter 成功后20分钟”两者中较早的时刻。/enter 成功响应中的 remaining_real_duration_s 是本局实际还能使用的完整现实时间秒数，范围为0至1200；机器狗程序应使用该值，不应固定假定每次都有1200秒。
P114: 虚拟世界限时为360000秒。达到现实时间或虚拟时间截止条件后，模拟器结束测试并关闭机器狗接口。截止前已经完整到达并被登记的唯一动作允许完成；在截止时刻或之后才完整到达的请求不再执行，连接可能直接关闭。测试结束后不能再调用 /exit 查询原因，具体结束原因由模拟器界面显示。
P116: 5. 通信格式总规则
P117: 5.1. 请求格式
P118: 4条指令都使用 HTTP+JSON。
P119: 方法：POST
P120: 请求头：Content-Type 必须是 application/json，可以带且只允许参数 charset=utf-8。出现其他参数时返回 HTTP 415。
P121: Content-Encoding 可以省略，或为 identity。其他值返回 HTTP 415。
P122: 请求体必须是无 BOM 的 UTF-8 JSON 对象，不能有重复键；JSON 嵌套不超过16层；请求体最大65536字节。
P123: 路径必须精确为 /enter、/measure、/clear 或 /exit，不接受尾随斜线和查询参数。
P124: 请求体必须是UTF-8 JSON对象，不能有重复键；请求体最大65536字节。
P125: channel 必须是 1 至 20 的整数。写成 JSON number 1.0 且数值恰好为整数时也会被接受；如写成1.5 则返回 HTTP 400。
P126: 未声明的JSON字段不会被忽略，而是返回HTTP 200和 accepted=false，用于帮助发现字段拼写错误。
P127: arena_id 必须是ASCII字符串“default”。
P128: robot_id 必须逐字节等于当前登录参赛队号，UTF-8长度为1至64字节。
P129: request_id 是当前测试会话内的幂等键，UTF-8长度为1至128字节。robot_id 和 request_id 不能包含控制字符或不可见格式字符。
P130: 5.2. 响应格式
P131: 业务响应也是 JSON 对象。
P132: 所有 JSON 业务响应都有以下字段：
T133R0: 字段 | 类型 | 含义
T133R1: accepted | boolean | 模拟器是否接受本次请求。
T133R2: real_timestamp_ms | number | 模拟器响应时的现实世界时间戳，单位为毫秒。
T133R3: virtual_time_s | number | 虚拟时钟，单位为秒。
P134: 如果 accepted=false，响应只包含上面 3 个字段，不包含检测结果、清除结果或退出原因。
P135: 5.3. HTTP状态和业务接受状态
T136R0: HTTP状态 | 含义
T136R1: 200，accepted=true | 动作已执行。
T136R2: 200，accepted=false | JSON结构合法，但当前测试状态拒绝动作；或存在未知字段；或 arena_id、robot_id 不匹配。
T136R3: 400 | JSON语法、重复键、缺失字段、字段类型、标识符格式、频道或坐标范围错误。
T136R4: 404 | 路径未知或路径不精确。
T136R5: 405 | 已知路径使用了非POST方法。
T136R6: 409 | 同一 request_id 对应了不同动作，或并发发送了不同动作。
T136R7: 413 | 请求体超过65536字节。
T136R8: 415 | Content-Type 或 Content-Encoding 不受支持。
T136R9: 429 | 连接/无效流量保护触发，或本局幂等记录达到上限。
T136R10: 500 | 模拟器内部错误。
P137: 所有能够形成HTTP响应的错误都返回JSON，其中至少包含 accepted、real_timestamp_ms 和 virtual_time_s。机器狗程序必须同时检查HTTP状态和 accepted，不能只判断其中一个。
P138: 每个新动作必须使用新的 request_id。只有在网络超时、连接中断等情况下重试完全相同的动作时，才复用原请求内容和原 request_id。同一ID、同一内容返回第一次的完整响应，不重复移动、检测、清除、退出或推进时间；同一ID改动路径、位置、频道等内容返回HTTP 409。结构错误、未知字段以及 arena_id、robot_id 不匹配不占用该ID，修正请求后可以复用。正常串行合法动作不设速率上限，但不同新动作不得并发发送。
P139: 并非所有错误都能形成HTTP响应。倒计时未结束、接口尚未开放、测试已经结束，或模拟器内部无法继续服务时，连接可能被直接关闭，此时没有JSON体。机器狗程序必须同时处理“连不上”和“收到JSON”。
P140: 结构错误（HTTP 400）以及未知字段、arena_id / robot_id 不匹配（HTTP 200且 accepted=false）不占用该 request_id，修正后可以复用。业务上已被接受的动作占用该 request_id。
P142: 6. POST /enter：进入目标区域
P143: 6.1. 请求
P144: 发送字段：
T145R0: 字段 | 类型 | 必填 | 取值说明
T145R1: arena_id | string | 是 | 必须是 "default"。
T145R2: robot_id | string | 是 | 机器狗标识，非空字符串，参赛队号。
T145R3: request_id | string | 是 | 请求标识，非空字符串。
P146: 请求示例：
P147: {  "arena_id": "default",  "robot_id": "<参赛队号>",  "request_id": "enter-1"}
P148: 6.2. 响应
P149: accepted=true 时，返回字段：
T150R0: 字段 | 类型 | 取值说明
T150R1: accepted | boolean | true。
T150R2: real_timestamp_ms | number | 现实世界时间戳，单位为毫秒。
T150R3: virtual_time_s | number | 进入后的虚拟时刻；/enter 不推进虚拟时钟。
T150R4: max_virtual_duration_s | number | 模拟器配置的虚拟世界限时，默认 360000。
T150R5: max_real_duration_s | number | 模拟器配置的现实世界限时，默认 1200。
T150R6: remaining_real_duration_s | number | 本次测试从 /enter 成功起实际还能使用的完整现实时间秒数，整数范围 {0,1,2,⋯,1200}。
P151: 响应示例：
P152: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 0,  "max_virtual_duration_s": 360000,  "max_real_duration_s": 1200,  "remaining_real_duration_s": 1200}
P153: 6.3. 请求未执行的常见原因
P154: HTTP 200且 accepted=false：
P155: arena_id 不是 "default"。
P156: robot_id 与当前登录参赛队号不一致。
P157: 请求包含未声明字段。
P158: 重复调用 /enter。
P159: HTTP 400：
P160: 请求体不是合法 JSON，或含有重复键。
P161: 缺少 arena_id、robot_id 或 request_id。
P162: robot_id 或 request_id 为空、超长，或含控制/格式字符。
P163: 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应。
P165: 7. POST /measure：检测
P166: 7.1. 请求
P167: 发送字段：
T168R0: 字段 | 类型 | 必填 | 取值说明
T168R1: arena_id | string | 是 | 必须是 "default"。
T168R2: robot_id | string | 是 | 机器狗标识，非空字符串，参赛队号。
T168R3: request_id | string | 是 | 请求标识，非空字符串。
T168R4: position | object | 是 | 检测位置。
T168R5: position.x | number | 是 | X 坐标，有限JSON number，单位米，绝对值不得超过2000000。
T168R6: position.y | number | 是 | Y 坐标，有限JSON number，单位米，绝对值不得超过2000000。
T168R7: channel | number | 是 | 检测频道，整数范围 {1,2,⋯,20}。
P169: 请求示例：
P170: {  "arena_id": "default",  "robot_id": "<参赛队号>",  "request_id": "measure-1",  "position": {"x": 300, "y": 400},  "channel": 1}
P171: 7.2. 响应字段
P172: accepted=true 时，返回字段：
T173R0: 字段 | 类型 | 取值说明
T173R1: accepted | boolean | true。
T173R2: real_timestamp_ms | number | 现实世界时间戳，单位为毫秒。
T173R3: virtual_time_s | number | 本次检测完成后的虚拟时刻。
T173R4: measure_result | string | 检测结果，取值为 "no_signal"、"near" 或 "direction"。
T173R5: svd_deg | number | 示向度，单位为度，保留两位小数。只有 measure_result="direction" 时返回。
P174: 7.3. 三种检测结果
P175: 无信号：
P176: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 105,  "measure_result": "no_signal"}
P177: 含义：当前位置接收不到该频道的干扰源信号。可能原因包括该频道没有未清除干扰源、距离超过最大可被有效接收距离、定向干扰源没有覆盖该位置等。
P178: 距离过近：
P179: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 105,  "measure_result": "near"}
P180: 含义：已检测到该频道信号，但距离干扰源不超过 5 米。此时不返回 svd_deg。
P181: 正常返回示向度：
P182: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 105,  "measure_result": "direction",  "svd_deg": 123.45}
P183: 含义：已检测到该频道信号，并返回从检测点指向干扰源的方位角。svd_deg 保留两位小数，包含误差，参见题目正文。
P184: 7.4. 请求未执行的常见原因
P185: HTTP 200且 accepted=false：
P186: 尚未成功调用 /enter。
P187: arena_id 不是 "default"。
P188: robot_id 与当前登录参赛队号不一致。
P189: 请求包含未声明字段，包括 position 下的未知字段。
P190: 机器狗已经退出，或测试已经因超时等原因结束。
P191: HTTP 400：
P192: 请求体不是合法 JSON，或含有重复键。
P193: 缺少 position、position.x、position.y 或 channel。
P194: position.x 或 position.y 不是有限数值（含 NaN、正负无穷大），或绝对值超过2000000。
P195: channel 不在 1..20 范围内，或不是整数。
P196: robot_id 或 request_id 格式不合法。
P197: 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应
P201: 8. POST /clear：清除
P202: 8.1. 请求
P203: 发送字段：
T204R0: 字段 | 类型 | 必填 | 取值说明
T204R1: arena_id | string | 是 | 必须是 "default"。
T204R2: robot_id | string | 是 | 机器狗标识，非空字符串，参赛队号。
T204R3: request_id | string | 是 | 请求标识，非空字符串。
T204R4: position | object | 是 | 清除位置。
T204R5: position.x | number | 是 | X 坐标，有限JSON number，单位米，绝对值不得超过2000000。
T204R6: position.y | number | 是 | Y 坐标，有限JSON number，单位米，绝对值不得超过2000000。
T204R7: channel | number | 是 | 要尝试清除的目标干扰源频道，整数范围 {1,2,⋯,20}。
P205: 请求示例：
P206: {  "arena_id": "default",  "robot_id": "<参赛队号>",  "request_id": "clear-1",  "position": {"x": 300, "y": 0},  "channel": 3}
P207: 8.2. 清除规则
P208: 清除半径是 20 米。
P209: 如果指定频道的未清除干扰源距离 position 小于等于 20 米，则清除成功。注意，无论全向还是定向干扰源，只要指定频道的干扰源距离小于等于 20 米，都能够精确定位并清除，不受定向干扰源有效覆盖角度范围的限制。
P210: 如果该频道不存在干扰源、干扰源已经被清除、或干扰源距离超过 20 米，都视为未发现可清除目标。
P211: 同一个干扰源只能清除一次。重复清除已被清除的干扰源，会返回未发现可清除目标。
P212: /clear 不会切换测向机当前频道。
P213: 8.3. 响应字段
P214: accepted=true 时，返回字段：
T215R0: 字段 | 类型 | 取值说明
T215R1: accepted | boolean | true。
T215R2: real_timestamp_ms | number | 现实世界时间戳，单位为毫秒。
T215R3: virtual_time_s | number | 本次清除完成后的虚拟时刻。
T215R4: clear_result | string | 清除结果码。只有 "success" 和 "no_target_in_range"。
P217: 8.4. 两种清除结果
P218: 成功清除：
P219: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 196,  "clear_result": "success"}
P220: 对应含义：
P221: clear_result="success"。
P222: 清除动作耗时为 5 秒，再加移动耗时。
P223: 未发现可清除目标：
P224: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 194,  "clear_result": "no_target_in_range"}
P225: 对应含义：
P226: clear_result="no_target_in_range"。
P227: 清除动作耗时为 3 秒，再加移动耗时。
P228: 8.5. 请求未执行的常见原因
P229: 与 /measure 相同，见第7.4节。/clear 的 channel 表示要尝试清除的目标干扰源频道，同样必须是 1..20 的整数；该字段不会切换测向机当前频道。
P231: 9. POST /exit：退出目标区域
P232: 9.1. 请求
P233: 发送字段：
T234R0: 字段 | 类型 | 必填 | 取值说明
T234R1: arena_id | string | 是 | 必须是 "default"。
T234R2: robot_id | string | 是 | 机器狗标识，非空字符串，参赛队号。
T234R3: request_id | string | 是 | 请求标识，非空字符串。
P235: 请求示例：
P236: {  "arena_id": "default",  "robot_id": "<参赛队号>",  "request_id": "exit-1"}
P237: 9.2. 响应
P238: accepted=true 时，返回字段：
T239R0: 字段 | 类型 | 取值说明
T239R1: accepted | boolean | true。
T239R2: real_timestamp_ms | number | 现实世界时间戳，单位为毫秒。
T239R3: virtual_time_s | number | 退出时的虚拟时刻；/exit 不推进虚拟时钟。
T239R4: exit_reason | string | 退出原因。
P240: exit_reason 可能取值：
T241R0: 取值 | 含义
T241R1: user_exit | 主动调用 /exit 退出。
P242: /exit 与其他动作使用相同的 request_id 幂等规则。超时和手工中止会直接结束测试并关闭接口，不通过事后 /exit 返回原因。
P243: 响应示例：
P244: {  "accepted": true,  "real_timestamp_ms": 1760000000000,  "virtual_time_s": 199,  "exit_reason": "user_exit"}
P245: 9.3. 请求未执行的常见原因
P246: HTTP 200且 accepted=false：
P247: 尚未成功调用 /enter。
P248: arena_id 不是 "default"。
P249: robot_id 与当前登录参赛队号不一致。
P250: 请求包含未声明字段。
P251: HTTP 400：
P252: 请求体不是合法 JSON，或含有重复键。
P253: 缺少 arena_id、robot_id 或 request_id。
P254: robot_id 或 request_id 格式不合法。
P255: 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应。不要在测试结束后调用 /exit 查询原因。
P257: 10. 完整计时示例
P258: 本示例只说明指令含义和虚拟耗时计算，不代表查找策略。
P259: 指令序列：
T260R0: 步骤 | 指令 | 位置参数 | 频道参数含义 | 移动距离(m) | 移动耗时(s) | 切换频道耗时(s) | 检测/清除动作耗时(s) | 本步总耗时(s) | 虚拟时刻(s)
T260R1: 1 | /enter | - | - | 0 | 0 | 0 | 0 | 0 | 0
T260R2: 2 | /measure | (300,400) | 检测频道 1 | 500 | 100 | 0 | 5 | 105 | 105
T260R3: 3 | /measure | (300,400) | 检测频道 2 | 0 | 0 | 1 | 5 | 6 | 111
T260R4: 4 | /clear | (300,0) | 目标干扰源频道 3 | 400 | 80 | 0 | 3 | 83 | 194
T260R5: 5 | /measure | (300,0) | 检测频道 2 | 0 | 0 | 0 | 5 | 5 | 199
T260R6: 6 | /exit | - | - | 0 | 0 | 0 | 0 | 0 | 199
P261: 关键说明：
P262: 第 2 步从 (0,0) 到 (300,400)，移动距离为 500 米，移动耗时 100 秒；检测频道为 1，与初始频道相同，不需要切换频道。
P263: 第 3 步位置不变，但检测频道从 1 改为 2，产生 1 秒切换频道耗时。
P264: 第 4 步是 /clear，频道 3 是目标干扰源频道，不是测向机切换频道；本例假设未发现可清除目标，所以清除动作耗时为 3 秒。
P265: 第 5 步再次 /measure 频道 2，测向机仍保持第 3 步检测后的频道 2，所以没有切换频道耗时。
P267: 11. 示例程序
P268: 下面代码只演示如何发送 4 类请求并读取响应，不包含查找策略。
P269: Python版：
P270: import jsonfrom urllib.request import Request, urlopen# 修改为你们自己的参赛队号BASE_URL = "http://127.0.0.1:2026"ROBOT_ID = "<参赛队号>"def post(path, payload):    request = Request(        BASE_URL + path,        data=json.dumps(payload).encode("utf-8"),        headers={"Content-Type": "application/json"},        method="POST",    )    with urlopen(request, timeout=5) as http_response:        response = json.loads(http_response.read().decode("utf-8"))    print(path, response)    return responsedef base(request_id):    return {        "arena_id": "default",        "robot_id": ROBOT_ID,        "request_id": request_id,    }def action(request_id, x, y, channel):    payload = base(request_id)    payload["position"] = {"x": x, "y": y}    payload["channel"] = channel    return payloaddef main():    enter_response = post("/enter", base("enter-1"))    if enter_response.get("accepted") is not True:        print("进入失败")        return    remaining_time = enter_response["remaining_real_duration_s"]    print("本局可用现实时间：", remaining_time, "秒")    # 与第10节计时示例完全相同的四个动作。    actions = [        ("/measure", action("measure-1", 300, 400, 1)),        ("/measure", action("measure-2", 300, 400, 2)),        ("/clear", action("clear-1", 300, 0, 3)),        ("/measure", action("measure-3", 300, 0, 2)),    ]    for path, payload in actions:        # 如果因网络故障重试本动作，必须复用这个payload及其中的request_id。        response = post(path, payload)        if response.get("accepted") is not True:            print("请求未执行")            return        if path == "/measure":            if response["measure_result"] == "direction":                print("示向度：", response["svd_deg"], "度")            elif response["measure_result"] == "near":                print("距离过近，没有示向度")            else:                print("未测得信号")        else:            if response["clear_result"] == "success":                print("清除成功")            else:                print("清除位置附近没有目标")    exit_response = post("/exit", base("exit-1"))    if exit_response.get("accepted") is True:        print("退出原因：", exit_response["exit_reason"])main()
P271: Matlab版：
P272: function demo_robot()% 修改为你们自己的参赛队号。baseUrl = 'http://127.0.0.1:2026';robotId = '<参赛队号>';options = weboptions('MediaType', 'application/json', 'Timeout', 5);enterRequest = baseRequest(robotId, 'enter-1');enterResponse = post(baseUrl, '/enter', enterRequest, options);if enterResponse.accepted ~= true    fprintf('进入失败\n');    return;endremainingTime = enterResponse.remaining_real_duration_s;fprintf('本局可用现实时间：%d秒\n', remainingTime);% 与第10节计时示例完全相同的四个动作。actions = {    '/measure', actionRequest(robotId, 'measure-1', 300, 400, 1);    '/measure', actionRequest(robotId, 'measure-2', 300, 400, 2);    '/clear', actionRequest(robotId, 'clear-1', 300, 0, 3);    '/measure', actionRequest(robotId, 'measure-3', 300, 0, 2)};for i = 1:size(actions, 1)    path = actions{i, 1};    payload = actions{i, 2};    % 如果因网络故障重试本动作，必须复用这个payload及其中的request_id。    response = post(baseUrl, path, payload, options);    if response.accepted ~= true        fprintf('请求未执行\n');        return;    end    if strcmp(path, '/measure')        if strcmp(response.measure_result, 'direction')            fprintf('示向度：%.2f度\n', response.svd_deg);        elseif strcmp(response.measure_result, 'near')            fprintf('距离过近，没有示向度\n');        else            fprintf('未测得信号\n');        end    else        if strcmp(response.clear_result, 'success')            fprintf('清除成功\n');        else            fprintf('清除位置附近没有目标\n');        end    endendexitRequest = baseRequest(robotId, 'exit-1');exitResponse = post(baseUrl, '/exit', exitRequest, options);if exitResponse.accepted == true    fprintf('退出原因：%s\n', exitResponse.exit_reason);endendfunction request = baseRequest(robotId, requestId)request = struct( ...    'arena_id', 'default', ...    'robot_id', robotId, ...    'request_id', requestId);endfunction request = actionRequest(robotId, requestId, x, y, channel)request = baseRequest(robotId, requestId);request.position = struct('x', x, 'y', y);request.channel = channel;endfunction response = post(baseUrl, path, payload, options)response = webwrite([baseUrl path], payload, options);fprintf('%s  %s\n', path, jsonencode(response));end
P274: 12. 编程注意事项
P275: 先在模拟器中在线登录，启动测试，等待界面显示机器狗接口已经就绪。
P276: 使用当前登录参赛队号作为 robot_id。
P277: 每个新动作使用新的 request_id；只在重试完全相同动作时复用原ID和原请求内容。
P278: 逐次等待响应，不得并发发送不同动作；正常串行合法动作没有速率上限。
P279: 同时检查HTTP状态和 accepted。accepted=false 时动作没有生效，virtual_time_s=0 不是当前虚拟时刻。
P280: 使用 /enter 返回的 remaining_real_duration_s 控制现实运行时间，不要固定假定有1200秒。
P281: 每次检测增加的5秒是虚拟耗时，现实中不需要等待5秒。
P282: 只有 measure_result="direction" 时才读取 svd_deg，并且不是精确的真实方位，有误差。
P283: /clear 不切换测向机频道；只有成功的 /measure 可能会更新当前频道。
P284: no_signal 不等于附近一定没有干扰源；也可能是超出接收距离或不在定向覆盖范围内。
P285: 测试超时、中止或其他原因结束测试后接口会关闭；不要尝试调用 /exit 查询原因。
P286: 程序判断JSON取值时使用 direction、near、no_signal、success、no_target_in_range 和 user_exit 等实际字符串，不依赖界面中文说明。
P287: 机器狗程序应该自行记录测试过程中的指令序列、响应信息等内容，模拟器没有这一功能。
P288: 倒计时未结束、测试已经结束或接口未开放时，连接可能直接失败，没有JSON体。
P289: 加密日志文件上传上限为2MB，与机器狗接口协议无关；机器狗程序应避免异常高频循环。