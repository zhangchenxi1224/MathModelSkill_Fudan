> 本文件记录数据集评测之前的初期集成检查。当前完整结论见 reports/FINAL_REPORT.md。

# 小规模联调记录

日期：2026-09-12。本次目标是部署实验方案，不做新数据集上的算法选优。

- 11项针对性验证通过：阳性—阴性共享半径、历史阴性重用、P4背向保留、固定朝向一致性、清除失败约束、三反馈后验、费用计算、开放测点、随机合法反馈真值保留、细长区域，以及先验拟合接口。
- 已验证读取用户既有ZIP，可识别360个不同世界；没有对该360集运行新一轮大规模比较。
- 最终整局联调只用两个旧世界。baseline、geometry、three_feedback、probability、lookahead共10次运行全部完整清除；所有最终运行的区域真值检查均无违规。
- risk_cut另跑两次：P3清除11/12并明确返回incomplete_risk_cut，P4清除12/12。名义概率预算没有提供真实漏清保证，不应据此启用为默认策略。
- baseline逐局复现旧部署值：P3 3358.889802秒，P4 8820.484549秒。
- 最终联调结果为 runs/smoke_final/RESULTS.md、results.json。概率先验仍未校准，这两局不能支持选优或提升率结论。
- runs/geometry_smoke、runs/planning_smoke为早期联调记录。后一批发现一次细长区域凸包包含异常；已修复近共线叉积判断，并避免无排除时重复重建凸包。最终整局复跑与对应回归验证通过。旧记录保留，不并入最终通过数量。

下一步：收到数据后先比较baseline/geometry/three_feedback，再使用训练先验比较probability/lookahead；risk_cut单独报告清除率与时间。最终候选用新的未参与选择的场景确认。
